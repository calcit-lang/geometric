# Upgrade geometric to Calcit 0.13.77

Agent: `codex-01a06d75-fd53-7c21-a61b-6966447aee4d-upgrades-6f2b`

- Migrated the legacy `package.cirru` manifest to `deps.cirru` and aligned the
  compiler plus `@calcit/procs` to exact version 0.13.77.
- Recovered the last runnable quoted Snapshot from `compact.cirru`, applied the
  official one-way formatter, and removed the retired duplicate Snapshot.
- Replaced archived `calcit-test` usage with built-in `calcit.test/is` while
  preserving every existing assertion.
- Replaced the removed `defrecord!` method bags with named positional enums and
  origin-carrying traits/impls. The `:ga3`/`:v3` payload order, public constructor
  functions, arithmetic implementations, and dot methods remain unchanged.
- Made the three `nth` calls explicit Option unwraps after the existing list and
  length assertions.
- Added explicit trait-origin and method-table assertions. The existing method
  results are checked against the direct arithmetic functions through
  `&trait-call`, avoiding unresolved dynamic dispatch while retaining the public
  method table.
- Added a definition-scoped quality baseline for the existing 19 untyped
  definitions and 15 deprecated `tag-match` calls. `codeDynamic`, `codeNil`,
  `declaredOptional`, and `unsafeCoerce` remain zero, and CI rejects any growth.
- Updated CI to the maintained setup action, immutable Yarn 4.12.0 install, exact
  toolchain verification, canonical Snapshot check, static diagnostics, and both
  native and JavaScript test paths.

Initial exact-0.13.77 native validation passed after the migration. The canonical
Snapshot SHA-256 at this checkpoint is
`cb3b47d4a486a107e16493aa976033c09cf2ce8f4f4a148b54d2f4b495de66e4`.

中文：本次迁移保持几何代数表示、算法、构造器与方法行为不变，移除旧 manifest、
retired Snapshot 和 archived 测试模块，并改用 0.13.77 的 nominal enum/trait、
Option 契约、维护中的 CI 安装器与 immutable Yarn。
